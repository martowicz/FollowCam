## Brain

Należy otworzyć sobie to w vscode i zrobić venva
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Upewnić się że w main jest IP maliny: \
`STREAM_URL = "http://192.168.X.X:8080/stream"` \
Uruchomić:
```bash
python3 main.py
```